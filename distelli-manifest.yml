release-engineering/packaging-build:
# This Basic Distelli Manifest assumes the following has been provisioned on
# the destination deploy server:
# OS: Ubuntu
  PkgInclude:
    - bdd/*.rb
    - bdd/*.log

  Start:
    - echo "Begin Start within distelli-manifest.yml"
    - time ./bdd/codetest.rb -r 1.9.3
    - echo "End Start within distelli-manifest.yml"
